services:
  app:
    build: .
    ports:
      - "80:8080"
    env_file:
      - .env
    environment:
      - TZ=Asia/Kolkata
      - SPRING_DATA_MONGODB_URI=${SPRING_DATA_MONGODB_URI}
    restart: always
